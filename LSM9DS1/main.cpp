#include <signal.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#include "comm/comm.h"


int main(int argc, char* argv[]) {


    // clean up 2 threads, imu and producer (socket)

    // Closes our socket; server sees EOF. Send closing message before that?
    exit(EXIT_SUCCESS);
}
